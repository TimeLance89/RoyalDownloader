# Royal Downloader configuration template.
# The first-run wizard creates .env from this file automatically.

# desktop: normal Windows/macOS/Linux computer, local browser and local binding.
# nas: persistent server/container operation through start.sh or Docker Compose.
ROYAL_DEPLOYMENT_MODE=nas
HOST=0.0.0.0
OPEN_BROWSER=0
DNS_OVERRIDE=1

TZ=Europe/Berlin

# Unprivileged container identity. Use the owner of runtime/data/media on the NAS.
PUID=1000
PGID=1000

# Absolute NAS directories. Docker must have write access to both destinations.
MOVIES_HOST_DIR=/volume1/media/Filme
SERIES_HOST_DIR=/volume1/media/Serien

# Optional fallback account. The first-run wizard creates a hashed account.
# APP_REQUIRE_AUTH is retained for old files but Royal now fails closed even if
# this value is set to false. Use *_FILE for mounted Docker/Kubernetes secrets.
APP_USERNAME=
APP_PASSWORD=
APP_PASSWORD_FILE=
APP_REQUIRE_AUTH=true

# First-run ownership. Leave empty to generate a high-entropy one-time token in
# data/FilmeDownloader/setup_bootstrap.json (0600) and print it to the server log.
# You can instead supply a pre-provisioned secret directly or from a mounted file.
ROYAL_SETUP_TOKEN=
ROYAL_SETUP_TOKEN_FILE=

# Network boundary.
ROYAL_BIND_ADDRESS=0.0.0.0
# LAN IPs, localhost and single-label NAS hostnames work without configuration.
# A public reverse-proxy hostname must be explicitly listed, e.g. royal.example.com.
# Named public entries also define the external HTTPS origin for tunnels, while
# forwarded client-IP headers still require ROYAL_TRUSTED_PROXIES.
ROYAL_ALLOWED_HOSTS=
# CIDRs of reverse proxies that are allowed to supply Forwarded metadata.
# Loopback is trusted automatically. Example: 172.18.0.0/16,10.0.0.20/32
ROYAL_TRUSTED_PROXIES=
# Enable only if the trusted proxy really supplies these respective headers.
TRUST_CLOUDFLARE_HEADERS=false
TRUST_X_FORWARDED_FOR=false
# Central request and live-channel resource limits.
ROYAL_MAX_API_BODY_BYTES=2097152
ROYAL_WS_MAX_CONNECTIONS_PER_CLIENT=4
ROYAL_WS_MAX_MESSAGE_BYTES=4096
ROYAL_WS_MESSAGES_PER_MINUTE=30

# Optional defaults for the onboarding wizard.
JELLYFIN_URL=http://192.168.1.10:8096
JELLYFIN_API_KEY=
JELLYFIN_API_KEY_FILE=
JELLYFIN_USER_ID=
JELLYFIN_USER_NAME=
TMDB_API_KEY=
TMDB_API_KEY_FILE=
TMDB_LANGUAGE=de-DE

# Interface and translation
UI_LANGUAGE=de
# Optional self-hosted LibreTranslate base URL instead of the public fallback.
UI_TRANSLATOR_URL=
UI_TRANSLATOR_API_KEY=
UI_TRANSLATOR_API_KEY_FILE=

# Optional provider mirror overrides.
SFLIX_BASE_URL=https://sflix.win
RIDOMOVIES_BASE_URL=https://ridomovies.su

# Serienanbieter-Schutz: 15, 30, 60 Minuten, danach exponentiell bis 6 Stunden.
SERIES_PROVIDER_COOLDOWN_INITIAL_SECONDS=900
SERIES_PROVIDER_COOLDOWN_MAX_SECONDS=21600
SERIES_PROVIDER_COOLDOWN_MULTIPLIER=2
SERIES_PROVIDER_FALLBACK_CACHE_TTL_SECONDS=1800
SERIES_PROVIDER_TRANSIENT_ERROR_TTL_SECONDS=60
SERIES_FALLBACK_RETRY_SECONDS=90
SERIES_RESOLVED_LINK_CACHE_TTL_SECONDS=900
SERIES_RESOLVED_LINK_CACHE_MAX_ENTRIES=2000

# Telegram
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_TOKEN_FILE=
TELEGRAM_CHAT_ID=

# Seerr and Moonfin. Seerr's host UI is loopback-only by default; set
# SEERR_BIND_ADDRESS=0.0.0.0 only when you intentionally expose it to the LAN.
SEERR_ENABLED=false
SEERR_URL=http://seerr:5055
SEERR_API_KEY=
SEERR_API_KEY_FILE=
SEERR_POLL_INTERVAL_SECONDS=60
SEERR_BIND_ADDRESS=127.0.0.1

# Automation
AUTO_DOWNLOAD=false
CHECK_INTERVAL_MIN=30
DL_WINDOW_START=
DL_WINDOW_END=
MOVIE_SUBSCRIPTION_PROBE_BUDGET_SECONDS=75

# Public Royal Downloader repository used by the updater.
UPDATE_GITHUB_REPOSITORY=TimeLance89/RoyalDownloader
UPDATE_CHANNEL=stable
UPDATE_GITHUB_BRANCH=main
UPDATE_GITHUB_TOKEN=
UPDATE_GITHUB_TOKEN_FILE=
UPDATE_MODE=manual
AUTO_UPDATE_INTERVAL_HOURS=6
YTDLP_AUTO_UPDATE=true
YTDLP_UPDATE_INTERVAL_HOURS=24
YTDLP_UPDATE_START_DELAY_SECONDS=300

# Network and transfer tuning
DNS_PRIMARY=1.1.1.1
DNS_SECONDARY=9.9.9.9
HLS_CONCURRENT_FRAGMENTS=8
MP4_HTTP_CHUNK_SIZE=4M
SLOW_DOWNLOAD_MIN_KIBPS=384
SLOW_DOWNLOAD_GRACE_SECONDS=45
SLOW_DOWNLOAD_WINDOW_SECONDS=90
# Keep this much free space on the destination filesystem before starting a job.
# Set to 0 to disable the reserve check.
DOWNLOAD_MIN_FREE_GIB=5
